This server accepts requests to upload files. The files will be stored in ./uploads
directory

This server can run on a remote host. By default, this server runs at localhost:4501

Run

```
npm ci
npm start
```
